<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.andik.solofood">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/original"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/original"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" android:label="Login" />

        <activity android:name=".MainActivity" android:label="Home" />

        <activity android:name=".Detail" android:label="Detail" />

    </application>

</manifest>